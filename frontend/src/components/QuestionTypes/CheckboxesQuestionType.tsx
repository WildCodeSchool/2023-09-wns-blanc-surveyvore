import { Question } from "@/types/question.type";

export default function CheckboxesQuestionType({
    questions,
    setQuestions,
    questionId,
}: {
    questions: Question[];
    setQuestions: (questions: Question[]) => void;
    questionId: string;
}) {
    return (
        <>
        
        </>
    );
}
