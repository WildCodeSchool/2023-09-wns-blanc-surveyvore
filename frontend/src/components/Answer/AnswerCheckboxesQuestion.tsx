import { Question } from "@/types/question.type";

function AnswerCheckboxesQuestion({ question }: { question: Question }) {
  console.log(question)
  return <p>checkboxes Question</p>
}

export default AnswerCheckboxesQuestion;
